# Route.100SaaS (Lead Router)

**Tool 40**

## What’s in here

- PocketBase backend (hooks + migrations): `tools/route/pocketbase/`
- Shared PocketBase kernel: `kernel/pocketbase/_shared/pb_hooks/_shared/kernel.js`

## Run locally (quickstart)

```bash
node scripts/pb/run.mjs route
```

Then open:
- Admin UI: `http://127.0.0.1:8090/_/`
- API base: `http://127.0.0.1:8090/api/`

## Schema + endpoints

- Schema: `tools/route/pocketbase/SCHEMA.md`
- Tool notes: `tools/route/pocketbase/README.md`
